// Virtual aggregator for local @selfxyz/common when dist isn't built
// Re-export EVERYTHING from local source to avoid missing symbol issues in Storybook
export * from '../../.local-packages/common/index.ts';
